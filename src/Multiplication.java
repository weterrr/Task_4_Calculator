class Multiplication {
    double multiNum;

    void multiplic (double firstNum, double secondNum) {
        multiNum = firstNum * secondNum;
        System.out.printf("Произведение равно: %.3f", multiNum);
    }
}