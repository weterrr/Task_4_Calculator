class Substraction {
    double subNum;

    void substract(double firstNum, double secondNum) {
        subNum = firstNum / secondNum;
        System.out.printf("Частное равно: %.3f", subNum);
    }
}